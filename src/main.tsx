import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";
// Using real Scent Studio bottle photos as universal product images

createRoot(document.getElementById("root")!).render(<App />);
